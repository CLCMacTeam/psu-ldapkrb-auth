# psu-ldapkrb-auth
Configuration files for configuring PSU authentication for macOS 

These files are used in conjunction with the PSUAuth.bigfix AutoPkg recipe. The recipe builds a pkg from these files and imports it into BigFix. 